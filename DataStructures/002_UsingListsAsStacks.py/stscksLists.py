# last in, first out

stacks = [3, 4, 5] # using lists as stacks
print(stack)

stacks.append(1) # append, add item to last stack list
print(stacks)

stacks.pop() # pop, remove item from last stack lists
print(stacks)
